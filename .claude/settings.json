{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "PATH=/opt/homebrew/bin:/usr/local/bin:$PATH jq -r '.tool_input.file_path' | { read -r f; [[ \"$f\" == *.py ]] && ruff format \"$f\" && ruff check --fix \"$f\"; } 2>>~/.claude/hook-errors.log || true"
          }
        ]
      }
    ]
  }
}
