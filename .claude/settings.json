{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; [[ \"$f\" == *.py ]] && ruff format \"$f\" && ruff check --fix \"$f\"; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
