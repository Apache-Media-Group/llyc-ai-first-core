[1mdiff --git a/main.py b/main.py[m
[1mindex 7cc6233..305ff4b 100644[m
[1m--- a/main.py[m
[1m+++ b/main.py[m
[36m@@ -45,9 +45,6 @@[m [mfrom tools import meta, google_ads, ga4, drive, notifications, shopify[m
 from tools.email import render_email_html[m
 from tools.definitions import get_tool_definitions  # DEC_065[m
 from prompt_builder import load_static_prompt, build_dynamic_context  # F-CloudLog (29/05)[m
[31m-from operational_inputs import (  # DEC_075[m
[31m-    load_operational_inputs, to_prompt_block, reference_used,[m
[31m-)[m
 [m
 # ─── BOOTSTRAP ────────────────────────────────────────────────────────────────[m
 # CRÍTICO: no usar logging.basicConfig en Cloud Functions Gen 2.[m
[36m@@ -135,14 +132,14 @@[m [mdef load_client_config(client_id: str) -> dict:[m
     return config[m
 [m
 [m
[31m-def resolve_agent_id(config: dict, agent_name: str) -> str | None:[m
[32m+[m[32mdef resolve_agent_id(config: dict, agent_name: str) -> str:[m
     """[m
[31m-    Valida que el agente es invocable y devuelve su agent_id (solo para traza).[m
[32m+[m[32m    Devuelve config.agents[agent_name].agent_id.[m
[32m+[m[32m    Falla con error explícito si el agente no está bootstrapped o está disabled.[m
 [m
[31m-    DEC_076: el gate de activación es enabled + existencia en config.agents +[m
[31m-    system prompt presente. El agent_id ya no se usa en runtime (DEC_066) y puede[m
[31m-    ser None en agentes no bootstrapped; por eso ya NO se exige aquí. Se mantienen[m
[31m-    los checks de existencia y enabled.[m
[32m+[m[32m    DEC_065: el agent_id ya no se usa en runtime (Messages API no lo requiere),[m
[32m+[m[32m    pero se sigue resolviendo aquí como check de coherencia con el bootstrap[m
[32m+[m[32m    y para trazabilidad histórica en los logs.[m
     """[m
     client_id = config["client"]["id"][m
     agents = config.get("agents", {})[m
[36m@@ -164,8 +161,14 @@[m [mdef resolve_agent_id(config: dict, agent_name: str) -> str | None:[m
             f"Agent '{agent_name}' deshabilitado en config de '{client_id}'."[m
         )[m
 [m
[31m-    # DEC_076: agent_id solo para traza; puede ser None en agentes no bootstrapped.[m
[31m-    return agent_config.get("agent_id")[m
[32m+[m[32m    agent_id = agent_config.get("agent_id")[m
[32m+[m[32m    if not agent_id or agent_id in INVALID_VALUES:[m
[32m+[m[32m        raise RuntimeError([m
[32m+[m[32m            f"agent_id vacío o PENDIENTE para '{agent_name}' en '{client_id}'. "[m
[32m+[m[32m            f"Ejecutar bootstrap primero."[m
[32m+[m[32m        )[m
[32m+[m
[32m+[m[32m    return agent_id[m
 [m
 [m
 # ─── CARGA DE SECRETS ─────────────────────────────────────────────────────────[m
[36m@@ -915,22 +918,19 @@[m [mdef send_notification_for_agent([m
         subject_label = f"{execution_status} · {analysis_status}"[m
     subject = f"[{subject_label}] {agent_name} · {client_name} · {analysis_date}"[m
 [m
[31m-    # Contexto del template — agnostico de agente (DEC_079). Spread del output[m
[31m-    # completo para que cada plantilla acceda a SUS campos (period/budget_plan/[m
[31m-    # pacing/rentability budget-pacer; patterns/week/analysis_window weekly-digest;[m
[31m-    # totals naming-utm-auditor; platforms/revenue_triangulation/alerts perf-monitor).[m
[31m-    # Mas las claves inyectadas por el handler (no vienen del output) y las resueltas[m
[31m-    # (override de legacy/missing). Con StrictUndefined cada plantilla referencia[m
[31m-    # solo lo que su propio agente emite.[m
[32m+[m[32m    # Contexto del template — debe contener TODO lo que performance_monitor.html[m
[32m+[m[32m    # referencia. Si falta alguna key con StrictUndefined la render falla.[m
     context = {[m
[31m-        **output,[m
         "client_name": client_name,[m
         "analysis_date": analysis_date,[m
[31m-        "drive_url": drive_url,[m
         "execution_status": execution_status,[m
[31m-        "analysis_status": analysis_status,[m
         "execution_status_detail": execution_status_detail,[m
[32m+[m[32m        "analysis_status": analysis_status,[m
         "summary": summary,[m
[32m+[m[32m        "revenue_triangulation": output.get("revenue_triangulation"),[m
[32m+[m[32m        "platforms": output.get("platforms", {}),[m
[32m+[m[32m        "alerts": output.get("alerts", []),[m
[32m+[m[32m        "drive_url": drive_url,[m
     }[m
 [m
     template_name = f"{agent_name.replace('-', '_')}.html"[m
[36m@@ -1076,25 +1076,7 @@[m [mdef agent_executor(request):[m
         # cada invocación: static prompt del repo + contexto dinámico del config.[m
         static_prompt = load_static_prompt(agent_name)[m
         dynamic_context = build_dynamic_context(config, agent_name)[m
[31m-[m
[31m-        # DEC_075: capa de parámetros operativos. Lee el workbook operativo del[m
[31m-        # cliente (budget dinámico + tolerancias KPI) por Sheets API y lo inyecta[m
[31m-        # en el prompt. load_operational_inputs NUNCA lanza: degrada a fallback de[m
[31m-        # config y señala la fuente, de modo que un workbook caído no tumba la[m
[31m-        # ejecución (a lo sumo el agente reporta el fallback en su output).[m
[31m-        enabled_platforms = [[m
[31m-            k for k, v in config.get("platforms", {}).items()[m
[31m-            if isinstance(v, dict) and v.get("enabled")[m
[31m-        ][m
[31m-        oi = load_operational_inputs(config, agent_name, platforms=enabled_platforms)[m
[31m-        log.info(json.dumps({[m
[31m-            "event": "operational_inputs_loaded",[m
[31m-            "client_id": client_id,[m
[31m-            "agent": agent_name,[m
[31m-            **reference_used(oi),[m
[31m-        }))[m
[31m-[m
[31m-        system_prompt = f"{static_prompt}\n\n{dynamic_context}\n\n{to_prompt_block(oi)}"[m
[32m+[m[32m        system_prompt = f"{static_prompt}\n\n{dynamic_context}"[m
         tools = get_tool_definitions(agent_name.replace("-", "_"))[m
 [m
         # ── 7. Construir user_message e invocar el agent ──────────────────────[m
